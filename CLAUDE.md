# Claude 5 Sonnet 执行约束

> 适用对象：在本仓库中修改代码、测试、文档或配置的 Claude 5 Sonnet。
>
> 目标：在不破坏既有多人游戏和平台契约的前提下，持续维护 CS Party Arena 与 `cs-career` 职业生涯模拟器的一致架构、中文产品表达和战术档案视觉风格。

## 0. 指令优先级与执行方式

1. 系统、用户和运行环境指令优先于本文件。
2. 仓库规则按以下顺序解释：`AGENTS.md`、`docs/platform-api.md`、`docs/games/cs-career/RULES.md`、`design.md`、本文件、源码与测试。
3. 较高优先级与较低优先级冲突时，严格遵守较高优先级；不要通过改写高优先级文档消除冲突。
4. 需求不明确时，先阅读相关实现、测试和上述文档，再选择与既有模式一致的最小改动。仅在无法从仓库事实推断时向用户提问。
5. 不得把未验证的假设、半成品、伪实现或“后续再处理”当作完成。功能改动必须连同相关测试和验证一并完成。
6. 不得为满足当前需求重构无关模块、替换技术栈、改动包版本、格式化大量无关文件，或重命名稳定 API。

## 1. 仓库边界与稳定标识

- 本仓库同时包含多人 `CS推推` 和单人 `cs-career`；两者必须保持独立的玩法、存档、评分、房间和匹配边界。
- 下列职业生涯标识是持久化与平台分区键，禁止修改、复用或用硬编码替代：
  - `gameId: 'cs-career'`
  - `seasonId: 'career-v1'`
  - 路由：`/games/cs-career`
  - 存档键：`cspa:career:cs-career:v1`
- 多人游戏的 `GAME_ID = 'cs-push'` 和 `SEASON_ID = 'season-v1'` 同样是稳定标识。修复职业生涯功能时，不得改变其行为或契约。
- 所有账号、偏好、游戏发现、启动、评分、匹配、房间和大厅导航只能经由 `src/platform.ts` 导出的 `platform` / `PlatformAdapter` 访问。UI 组件不得直接读写平台 localStorage 键、直接派发导航事件或自行实现平台状态。
- 启动 manifest 游戏必须使用 `platform.launchGame()`；返回大厅必须使用 `platform.leaveToLobby()`。
- `cs-career` 是单人本地存档游戏：不得接入平台天梯、对局记录、快速匹配、房间、组队或平台评分。对单人 manifest 的这些操作必须被 adapter 拒绝。
- 平台数据契约发生变化前必须完整阅读 `docs/platform-api.md`，并同步更新 `src/platform.ts` 的类型、实现与聚焦 Vitest 测试。

## 2. 职业生涯引擎：确定性与纯函数

- `src/careerEngine.ts` 是职业生涯规则的唯一所有者，必须保持框架无关：不得导入 React、DOM、路由、音频、平台 adapter 或网络 API。
- 引擎状态转换必须是纯函数：相同版本、种子、初始输入和选择序列，必须产生完全相同的状态、赛季记录、事件顺序与结局。
- 任何职业生涯模拟路径中禁止调用 `Math.random()`、`Date.now()`、`new Date()`、`crypto.randomUUID()`、网络请求，或读取未持久化的全局可变状态。
- 新的随机分支必须从职业引擎的可复现伪随机数生成器派生；随机输入必须稳定地绑定种子、赛季及必要的选择/事件标识，避免调用顺序变化影响结果。
- 增加、删除或改变会影响同一种子结果的规则时，必须同时处理种子/存档版本兼容性。不得继续宣称旧版本种子可完全复现。
- UI 不得自行计算赛季结算、能力变化、退役条件、标题、事件选择或随机结果；它只能调用并渲染引擎导出的状态和转换函数。
- 每个事件选项必须有可读的短期代价提示，并在至少两类资源间形成取舍；禁止无提示的必死选项和单一明显最优选项。
- 四条核心数值保持为 `ability`、`connections`、`integrity`、`fame`，展示文案分别为“能力、关系、清白、名气”。修改数值、退役、年龄衰退、合同或事件规则前，先对照 `docs/games/cs-career/RULES.md`。

## 3. 存档与浏览器边界

- 职业生涯存档只能使用专用版本化键 `CAREER_SAVE_KEY`，与平台账号、偏好、评分、队列、房间和对局记录隔离。
- 读取 localStorage、JSON 解析和版本检查必须容错；缺失、损坏、类型不符或版本不匹配的职业生涯存档一律按“无存档”处理，不得让应用崩溃。
- 变更 `CareerState`、存档结构或版本时，必须明确旧存档策略：迁移、兼容读取，或提升版本并安全丢弃。不得让旧存档进入部分有效状态。
- 所有用户可见的职业生涯数据都必须来自持久化 state 或确定性引擎结果，不能依赖刷新后丢失的组件局部状态。

## 4. React 与 TypeScript 约束

- 使用现有 React 函数组件、hooks 和 TypeScript 类型风格；不引入状态管理库、CSS 框架、组件库或新路由库，除非用户明确要求。
- 组件职责保持单一：页面/组件负责交互和渲染，`careerEngine.ts` 负责模拟规则，`platform.ts` 负责平台适配，`audio.ts` 负责音频。
- 优先定义明确的联合类型、接口和 `Record`；禁止以 `any`、`@ts-ignore`、不安全类型断言或静默 catch 绕过类型与行为问题。
- 复用现有导出、常量和组件模式。新增抽象必须消除真实重复或复杂度，不得为少量一次性 JSX 提取泛化层。
- 交互必须使用语义化 `button`、`input`、`fieldset`、`label`、`dialog` 等元素；点击行为不能挂在不可聚焦 `div` 上。
- 图标按钮必须具有 `aria-label` 和 `title`；键盘焦点必须可见。需要图标时优先使用已安装的 `lucide-react`，不要手写无必要 SVG。
- 对重要且不可逆的动作保留明确的确认步骤。职业生涯主动退役只能在至少完成一个赛季结算后执行。
- 所有新增或修改的界面文案使用简体中文；允许保留现有的紧凑英文战术标签、数值缩写与产品标识。`cs-career` 允许使用 `src/careerData.ts` 中版本化的真实战队与选手静态快照；赛事方、赛事品牌、赞助商、生成角色和生成事件必须明确虚构。禁止运行时抓取实时阵容。

## 5. 视觉与 CSS 约束

### 全局和大厅

- 保持现有紧凑、硬边、战术面板语言：细边框、纸张/混凝土底色、网格、强对比区块和有限强调色。
- 保持已建立的 `Barlow Condensed` 用于数字和紧凑标题、`Noto Sans SC` 用于中文阅读文本的分工。
- 不得引入圆角卡片堆叠、玻璃拟态、渐变光球、大面积柔和阴影、营销式 hero 卡片或与现有视觉体系冲突的 SaaS 仪表盘风格。

### `cs-career` 档案模式

- 以 `design.md` 为职业生涯 UI 的强制视觉规范。核心颜色仅承担既定语义：近黑/枪灰为结构，暖纸色为底，橙色为主操作与 T 方强调，青色表示稳定数据，酸绿表示正向/干净状态，暗红表示风险。
- 页面应保持“编辑式选手档案 / 战术操作桌”质感：方形或 2px 以内的角、边框与间距代替浮动阴影、轻量网格与坐标标签。仅关键决策卡可使用硬偏移阴影。
- 不得把职业生涯页面改成卡片套卡片。三区档案布局在桌面为身份/主内容/状态，在窄屏按身份与状态、决策、四项轨道、赛季档案的阅读顺序垂直折叠。
- 颜色不能作为唯一含义。状态、结果和选择必须同时具有文本、数值、正负号或其他非颜色提示。
- 必须保持响应式可读性：文字不得溢出、遮挡或依赖固定超宽布局；窄屏至少覆盖 `850px` 断点下的职业模式布局。遵守 `prefers-reduced-motion`，不要无故添加持续动画。
- 修改 `src/styles.css` 时，只触及功能所属的样式区块；复用职业生涯现有 CSS 变量和命名空间，避免改变大厅或 `CS推推` 的共享样式。

## 6. 测试、验证与完成定义

- 修改 `src/careerEngine.ts` 时，必须在 `src/careerEngine.test.ts` 增加或更新对应的 Vitest 覆盖。至少覆盖确定性、有效/无效转移和被修改规则的边界条件。
- 修改 `src/platform.ts`、manifest、游戏启动、偏好、评分、队列或房间行为时，必须更新 `src/platform.test.ts`，验证平台契约与多人/单人隔离。
- 修改纯游戏规则时优先测试公共转换函数，不通过组件快照替代规则测试。
- 每次完成代码改动前，必须在仓库根目录依次运行：

```bash
npm run lint
npm test
npm run build
```

- 命令失败时必须修复与当前改动相关的问题后重跑；不得忽略、删除失败断言、降低测试强度或把失败报告为通过。
- 如果环境阻止运行某个命令，必须说明具体命令、阻塞原因及未验证范围；不能声称完成验证。
- 完成报告必须简洁列出：修改的文件、行为变化、执行过的验证及其结果、仍存在的真实限制。不要声称未执行的测试已通过。

## 7. 修改前检查清单

在提交任何代码改动前，逐项确认：

- 已阅读与目标模块对应的实现、测试和上位文档。
- 未修改 `cs-career`、`cs-push`、赛季、路由或存储键等稳定标识，除非用户明确要求迁移方案。
- 职业生涯新增随机性完全可复现，且不含 `Math.random()`。
- 平台能力仍只经由 `src/platform.ts` 使用，单人模式未进入匹配、房间、天梯或对局。
- 新增界面符合中文、可访问、低圆角、战术档案视觉和响应式要求。
- 对应的 Vitest 已更新，且 `npm run lint`、`npm test`、`npm run build` 均已执行并如实报告结果。
